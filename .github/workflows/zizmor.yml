name: zizmor workflow audits
on:
  push:
  pull_request:
  workflow_dispatch:


concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
   zizmor-workflow-audits:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: zizmor workflow audits
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          pip install zizmor
          go install github.com/buildkite/terminal-to-html/v3/cmd/terminal-to-html@latest

          zizmor . --gh-token $GITHUB_TOKEN | tee output.txt

          printf '%b\n' "\`\`\`" >> $GITHUB_STEP_SUMMARY
          cat output.txt | terminal-to-html -preview >> $GITHUB_STEP_SUMMARY
          printf '%b' "\`\`\`" >> $GITHUB_STEP_SUMMARY




